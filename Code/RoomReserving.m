%file: RoomReserving
%function: select the high MSB bits of pixels in the adjustment area and embedding them into the embedding area
function res = RoomReserving( ori, blocksize, MSB )
    res = ori;
    % TODO
end